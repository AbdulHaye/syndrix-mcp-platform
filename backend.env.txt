# ── App ──────────────────────────────────────────────────────────────────
APP_ENV=development
SECRET_KEY=dev-secret-change-me-in-production
API_HOST=0.0.0.0
API_PORT=8000

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql+asyncpg://mcp_user:mcp_pass@localhost:5432/syndrix

# ── Redis ──────────────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379/0

# ── Ollama (local LLM) ────────────────────────────────────────────────────────
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_DEFAULT_MODEL=llama3.2
OLLAMA_EMBED_MODEL=nomic-embed-text

# ── pgvector ──────────────────────────────────────────────────────────────────
# The pgvector extension is NOT installed on this machine, so embeddings are
# stored as JSON text and similarity falls back to recency/keyword ranking.
# Keep this "false" unless you install the vector extension in PostgreSQL.
PGVECTOR_ENABLED=false

# ── JWT Auth ──────────────────────────────────────────────────────────────────
JWT_SECRET_KEY=dev-jwt-secret-change-me-in-production
JWT_EXPIRE_MINUTES=480

# ── Bootstrap admin (auto-created on first startup if no admin exists) ────────
ADMIN_EMAIL=admin@syndrix.local
ADMIN_PASSWORD=changeme

# ── DEV_TOKENS — static fallback tokens for testing (format: team:token,...) ──
DEV_TOKENS=bd_team:bd-secret-token-123,dev_team:dev-secret-token-456,mgmt_team:mgmt-secret-token-789

# ── Langfuse (LLM observability/tracing — https://cloud.langfuse.com) ─────────
# Read directly by the langfuse SDK via os.environ.
LANGFUSE_SECRET_KEY="sk-lf-f8cbce86-6d89-43d4-b4e7-02a6ffd2f146"
LANGFUSE_PUBLIC_KEY="pk-lf-27681d35-8dc1-4b2e-b4ce-eb843824b3c4"
LANGFUSE_BASE_URL="https://us.cloud.langfuse.com"
LANGFUSE_TRACING_ENVIRONMENT=development
LANGFUSE_TRACING_ENABLED=true

# ── Phase 2 integration credentials (optional — fill in before wiring each) ───
PODIO_CLIENT_ID=
PODIO_CLIENT_SECRET=
PODIO_APP_ID=
GHL_API_KEY=
GHL_LOCATION_ID=
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=
GITHUB_TOKEN=
GITHUB_ORG=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# 1.28.0+ resolves string annotations before the Context issubclass check
# (older 1.x crashes on `from __future__ import annotations` in mcp_tools/*.py);
# <2.0.0 because mcp 2.x removed the mcp.server.fastmcp module this app is built on.
mcp>=1.28.0,<2.0.0
httpx>=0.27.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
redis>=5.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=24.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1/5.x
python-multipart>=0.0.9
celery>=5.3.0
pgvector>=0.3.0
numpy>=1.26.0
python-dotenv>=1.0.0
aiosmtplib>=3.0.0
langfuse>=4.14.0  # LLM observability/tracing — see app/services/tracing.py
cryptography>=42.0.0  # used directly in app/services/crypto.py, not just via python-jose extra
pytest>=8.0.0
pytest-asyncio>=0.23.0

Create a Firm Document
POST
https://external-integrations.mycase.com/v1/documents

Create an individual document not associated with a case.

Request
Body
application/json
application/json
path
string
required

The relative path of this document. This path should include the name of the document. Any folders listed in this path will be created automatically if they are not already in MyCase.

Example:
example_folder1/example_folder2/example_name
filename
string
required

The filename of the physical file associated with this document including type extension.

Example:
file.txt
description
string

The description of this document.

assigned_date
string

The assigned date of this document. Must be in ISO 8601 date format.

staff
array[object]

The staff that this document should be shared with.

id
integer
Responses
201

Created

Body
application/json
application/json
responses
/
201
id
integer
name
string

The name of the document as seen in MyCase.

filename
string

The name of the physical file associated with this document.

path
string

The folder structure of where this document is located in MyCase.

description
string
assigned_date
string
case
CaseNestedResponse or null
id
integer

The ID of the case.

created_at
string
updated_at
string
self_url
string

The url that can be used to view this document specifically.

Example:
/v1/documents/1
folder
FolderNestedResponse
id
number

The ID of the parent folder.

put_url
string

An Amazon S3 PUT url to upload your file.

put_headers
object

Include these headers with your S3 PUT request. Please make sure to set Content-Type: application/octet-stream

x-amz-acl
string
Request Sample: Shell / cURL
curl --request POST \
  --url https://external-integrations.mycase.com/v1/documents \
  --header 'Accept: application/json' \
  --header 'Content-Type: application/json' \
  --data '{
  "path": "example_folder1/example_folder2/example_name",
  "filename": "file.txt",
  "description": "string",
  "assigned_date": "string",
  "staff": [
    {
      "id": 0
    }
  ]
}'
Response Example
1
{
2
  "id": 0,
3
  "name": "string",
4
  "filename": "string",
5
  "path": "string",
6
  "description": "string",
7
  "assigned_date": "string",
8
  "case": {
9
    "id": 0
10
  },
11
  "created_at": "string",
12
  "updated_at": "string",
13
  "self_url": "/v1/documents/1",
14
  "folder": {
15
    "id": 0
16
  },
17
  "put_url": "string",
18
  "put_headers": {
19
    "x-amz-acl": "string"
20
  }
21
}
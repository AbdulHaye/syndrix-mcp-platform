Get Calls
GET
https://external-integrations.mycase.com/v1/calls

Get all firm calls viewable by the authorized user.

Request
Query Parameters
filter[updated_after]
string

Filters the response to return only resources that are created or updated after a specified date/time. ISO 8610 date format. Results are inclusive of the date/time entered. Can include time but does not need to.

Example:
?filter[updated_after]=2022-03-17T21:00:00Z
page_size
number

The ability to specify the number of resources that are returned per response page.

>= 1
<= 1000
Default:
25
Example:
?page_size=100
Responses
200

OK

Headers
Item-Count
number

The total number of results.

Link
string

If there is another page of results, we provides you with the cursor to the next page using page_token. Please reference our pagination documentation for more info.

Body
application/json
application/json
responses
/
200
array of:
id
number

A unique identifier for the call.

called_at
string

An ISO 8601 timestamp of when the call happened.

caller_phone_number
string

The caller's phone number.

call_for
object

The staff member that this call is associated with.

id
integer
case
object or null

The case that this call is associated with.

id
integer
lead
object or null

The lead that this call is associated with.

id
integer
message
string

A description of the call.

caller_name
string or null

The caller's name. This field is mutually exclusive with the client field.

client
object or null

The client that this call is associated with. This field is mutually exclusive with the caller_name field.

id
integer
call_type
string

One of "incoming" or "outgoing".

resolved
boolean

Whether or not this call is resolved.

created_at
string

The date the call was created in ISO 8601 date/time format.

updated_at
string

The date the call was last updated in ISO 8601 date/time format.

Request Sample: Shell / cURL
curl --request GET \
  --url https://external-integrations.mycase.com/v1/calls \
  --header 'Accept: application/json'
Response Example
1
[
2
  {
3
    "id": 0,
4
    "called_at": "string",
5
    "caller_phone_number": "string",
6
    "call_for": {
7
      "id": 0
8
    },
9
    "case": {
10
      "id": 0
11
    },
12
    "lead": {
13
      "id": 0
14
    },
15
    "message": "string",
16
    "caller_name": "string",
17
    "client": {
18
      "id": 0
19
    },
20
    "call_type": "string",
21
    "resolved": true,
22
    "created_at": "string",
23
    "updated_at": "string"
24
  }
25
]